const tokenService = require("../services/tokenService");
const dataBase = require("../services/database");

const create = (req, res) => {
    //Token Verify
    const tokenData = toke
};

module.exports = {
  create: create,
};
